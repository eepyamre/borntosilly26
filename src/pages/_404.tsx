import { Layout } from '@/components';

export const NotFound = () => {
  return (
    <Layout>
      <h1
        style={{
          fontSize: '10rem',
          textAlign: 'center',
        }}
      >
        404
      </h1>
    </Layout>
  );
};
